<Window x:Class="PenumbraOrganizer.App.Dialogs.FatalErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Penumbra Organizer"
        Width="560"
        Height="320"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#FFF4F6F8">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Penumbra Organizer could not start"
                       FontSize="22"
                       FontWeight="SemiBold" />
            <TextBlock Margin="0,12,0,0"
                       Text="{Binding Summary}"
                       TextWrapping="Wrap" />
            <TextBlock Margin="0,12,0,0"
                       Text="{Binding LogLocationLabel}"
                       Foreground="#FF556171"
                       TextWrapping="Wrap" />
        </StackPanel>

        <Expander Grid.Row="1"
                  Margin="0,16,0,0"
                  Header="Technical details">
            <TextBox Text="{Binding Details, Mode=OneWay}"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" />
        </Expander>

        <WrapPanel Grid.Row="2"
                   Margin="0,16,0,0"
                   HorizontalAlignment="Right">
            <Button Content="Open Logs Folder"
                    MinWidth="130"
                    Height="34"
                    Margin="0,0,10,0"
                    Click="OpenLogsFolder_Click" />
            <Button Content="Copy Details"
                    MinWidth="110"
                    Height="34"
                    Margin="0,0,10,0"
                    Click="CopyDetails_Click" />
            <Button Content="Close"
                    MinWidth="90"
                    Height="34"
                    IsDefault="True"
                    Click="Close_Click" />
        </WrapPanel>
    </Grid>
</Window>
